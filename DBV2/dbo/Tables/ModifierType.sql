CREATE TABLE [dbo].[ModifierType] (
    [ModifierTypeId]   INT           NOT NULL,
    [ModifierTypeName] VARCHAR (255) NOT NULL,
    CONSTRAINT [PK_ModifierType] PRIMARY KEY CLUSTERED ([ModifierTypeId] ASC)
);

